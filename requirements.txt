# Core
pandas>=2.0

# Live Fitbit integration (OAuth2 + Web API calls)
requests>=2.31

# Redmi / Xiaomi watch — Option A: BLE direct (real-time heart rate)
# Only needed for the BLE backend; Gadgetbridge (sqlite3, stdlib) and Zepp
# (requests, above) backends do not require it.
bleak>=0.21

# Voice analysis (Teams meeting audio → biomarkers)
# Uncomment as you implement each module.
# librosa>=0.10          # audio feature extraction (pitch, jitter, shimmer, energy)
# soundfile>=0.12        # read/write audio files
# moviepy>=1.0           # extract audio track from Teams .mp4 recordings
# numpy>=1.24
# scipy>=1.11

# Optional speech-to-text / diarization
# openai-whisper         # transcription
